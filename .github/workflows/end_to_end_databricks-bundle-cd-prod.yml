# This GitHub workflow deploys Bundle resources (ML resource config and more)
# defined under end_to_end_databricks/resources/*
# and end_to_end_databricks/databricks.yml with prod deployment target configs,
# when PRs are merged into the release branch
name: Bundle Deployment for end_to_end_databricks Prod

on:
  push:
    branches:
      - 'release'
    paths:
      - 'end_to_end_databricks/**'
      - '.github/workflows/end_to_end_databricks-bundle-cd-prod.yml'
  workflow_dispatch:

defaults:
  run:
    working-directory: ./end_to_end_databricks

env:
  DATABRICKS_HOST: https://dbc-314d456b-c68f.cloud.databricks.com
  DATABRICKS_TOKEN: ${{ secrets.PROD_WORKSPACE_TOKEN }}

jobs:
  prod:
    concurrency: end_to_end_databricks-prod-bundle-job
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: databricks/setup-cli@v0.236.0
      - name: Validate Bundle For Prod
        id: validate
        run: |
          databricks bundle validate -t prod
      - name: Deploy Bundle to Prod
        id: deploy
        run: |
          databricks bundle deploy -t prod
